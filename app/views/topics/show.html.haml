- title @topic.name

= render 'home/articles', articles: @topic.articles.visible

- if user_logged_in? && current_user.is_admin?
  = link_to 'Edit', [:edit, @topic], class: 'btn btn-info'
  \|
  = link_to 'Back', topics_path
