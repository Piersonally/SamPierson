= bootstrap_form_for @slide_show do |f|
  = f.input :title
  = f.input :slug
  = f.input :content, rows: 10

  .form-group
    .col-sm-offset-3.col-sm-2
      = f.submit 'Save', class: 'btn btn-primary'
